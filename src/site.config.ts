import defineConfig from "./libs/config";

export default defineConfig({
  title: "Yuang Wei",
  description: "Gogogog",
  domain: 'http://localhost:3000',
  navs: [{
    path: '/projects',
    name: 'Projects'
  }]
})